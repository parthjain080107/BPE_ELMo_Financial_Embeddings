# BPE_ELMo_Financial_Embeddings
using Bite Pair Encoding(BPE) for tokenization and generating contextual embeddings via ELMo(2-Layer bi-directional LSTM ) trained on Financial dataset(Financial Phrase Bank).  
